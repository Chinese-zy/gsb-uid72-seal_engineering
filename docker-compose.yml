services:
  web:
    build: .
    environment:
      - PORT=8080
    ports:
      - "8762:8080"
    healthcheck:
      test: ["CMD", "python", "healthcheck.py"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
